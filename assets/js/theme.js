/**
 * Theme JavaScript
 * 
 * Custom theme scripts that apply regardless of framework selection.
 */

(function () {
    'use strict';

    // DOM ready
    document.addEventListener('DOMContentLoaded', function () {
        // Your custom JS here
    });

})();